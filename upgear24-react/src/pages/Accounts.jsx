import React from 'react';import {Search,WalletCards} from 'lucide-react';import PageHeader from '../components/PageHeader';import {getData} from '../store/data';
export default function Accounts(){const c=getData('customers'); return <div><PageHeader icon={WalletCards} title="Payments Queue" subtitle="Real-time sync enabled"><div className="tabs"><button className="selected">Sales Orders (0)</button><button>Advance Payment (0)</button><button>Technician W (0)</button></div></PageHeader><div className="mini-toolbar"><div className="tabs"><button className="selected">◷ Pending</button><button>↶ History</button></div><div className="search small"><Search size={16}/><input placeholder="Search & Press Enter for history"/></div><input className="date-input" placeholder="dd-mm-yyyy"/></div><div className="empty-state"><div className="empty-icon">▣</div><h3>No Pending Orders</h3><p>Orders submitted from the floor will appear here instantly.</p></div><div className="customer-preview">{c.length>0&&<div className="small-card"><strong>Demo customer</strong><span>{c[0].name} · {c[0].phone}</span></div>}</div></div>}
