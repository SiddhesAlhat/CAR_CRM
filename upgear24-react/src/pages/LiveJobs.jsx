import React from 'react';import {CalendarDays,Search,Wrench,ArrowRight} from 'lucide-react';import PageHeader from '../components/PageHeader';import {getData} from '../store/data';import {Link} from 'react-router-dom';
export default function LiveJobs(){const jobs=getData('jobs'); const [tab,setTab]=React.useState('Active Jobs'); return <div><PageHeader icon={Wrench} title={<>Live Jobs <span className="pill blue-pill">{jobs.length} Active</span></>} subtitle="Active job cards for ongoing work, technician fitting, & advance payment orders."><div className="tabs"><button className={tab==='Active Jobs'?'selected':''} onClick={()=>setTab('Active Jobs')}>Active Jobs ({jobs.length})</button><button>Technician Working (0)</button><button>Advance Payment Orders (0)</button></div></PageHeader><div className="filterbar"><button className="outline-btn"><CalendarDays size={16}/>Date Filter</button><div className="search small"><Search size={16}/><input placeholder="Search..."/></div></div><div className="job-grid">{jobs.map(j=><div className="job-card" key={j.id}><div className="job-top"><span className="pill green-pill">JOB #{j.id}</span><span className="muted">◷ {j.time}</span><strong>🚗 {j.vehicle}</strong></div><hr/><div className="customer-line"><span>♙</span><strong>{j.customer}</strong></div><div className="contact">☎ {j.phone}<span className="plate">{j.plate}</span></div><div className="job-amount"><span>Total Amount:</span><strong>₹{j.amount.toLocaleString('en-IN')}</strong></div><div className="sold-by">Sold by: <span>{j.soldBy}</span></div><hr/><div className="job-bottom"><span>{j.products} product added</span><Link to="/new-job">View Details <ArrowRight size={14}/></Link></div></div>)}</div></div>}
