import React from 'react';import {Users,Plus} from 'lucide-react';import PageHeader from '../components/PageHeader';import Modal from '../components/Modal';import {getData,setData} from '../store/data';
export default function Staff(){const [rows,setRows]=React.useState(getData('staff'));const [open,setOpen]=React.useState(false);const [f,setF]=React.useState({name:'',email:'',password:'',role:'STAFF'});const save=()=>{const next=[...rows,{id:Date.now(),...f}];setRows(next);setData('staff',next);setOpen(false)};return <div><PageHeader icon={Users} title="Staff Management" subtitle="Create and manage staff accounts for your team"><button className="primary-btn" onClick={()=>setOpen(true)}><Plus size={16}/> Add Staff</button></PageHeader><div className="staff-count"><Users size={18}/> <strong>{rows.length}</strong> Staff Accounts</div><div className="table-wrap"><table><thead><tr><th>NAME</th><th>EMAIL</th><th>PASSWORD</th><th>ROLE</th><th>CREATED</th><th>ACTIONS</th></tr></thead><tbody>{rows.length===0?<tr><td colSpan="6" className="empty-row">No staff accounts</td></tr>:rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.email}</td><td>••••••••</td><td>{r.role}</td><td>26 Aug 2026</td><td>⋯</td></tr>)}</tbody></table></div><Modal open={open} onClose={()=>setOpen(false)} title="Add Staff"><div className="form-grid"><label>Name<input value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Email<input value={f.email} onChange={e=>setF({...f,email:e.target.value})}/></label><label>Password<input type="password" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/></label><label>Role<select value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option>STAFF</option><option>MANAGER</option><option>TECHNICIAN</option></select></label></div><button className="primary-btn full" onClick={save}>Create Account</button></Modal></div>}
